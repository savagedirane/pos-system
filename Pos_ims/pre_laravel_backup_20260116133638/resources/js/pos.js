// Minimal POS frontend JS placeholder
document.addEventListener('DOMContentLoaded', function () {
  const app = document.getElementById('pos-app');
  if (app) {
    app.innerHTML = '<div class="box">POS UI placeholder</div>';
  }
});
